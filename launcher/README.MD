# Types of launchers

## runtime
Default launcher with isolation process (need be compiled to use)

## minimal
Minimal startup with logger, network, config and console system

## debug
Testing only launcher (no isolation) recommended if you want a fast test in the IDE

/**
 * Provides a common understanding of the `Registrar` component in this package.
 *
 * A **Registrar** is a class or component responsible for:
 * - Registering items (classes, services, routes, plugins, listeners, etc.)
 * - Storing them in an internal registry (e.g., `Map`, `List`)
 * - Allowing retrieval or lookup by key (name, ID, type)
 *
 * Common examples:
 * - `ListenerRegistrar` – allows plugins to register listeners
 *
 * ---
 * ** Aclaración para hispanohablantes:**
 * El término "Registrar" suena extraño como sustantivo (Ej: ListenerRegistrar, CommandRegistrar), en programación se usa como anglicismo técnico.
 * La traducción conceptual sería **componente de registro** o **clase de registro**.
 *  * El verbo en español sigue siendo "registrar" (ej: "registrar un servicio").
 */
@file:JvmName("PackageInfo")
package codes.reactor.sdk.scanner.registrar
